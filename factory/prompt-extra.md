<!-- factory:template - delete this line once you have real rules here
     caveman: pasted into the agy build prompt as-is, every run.
     orders for the builder only. delete this file if you have none.
     keep it to a few lines - long prompts get skimmed, by models too.

     good lines here:
       - never edit files under db/migrations/, they are generated
       - always add a type hint on new public functions
       - if you touch the parser, run scripts/regen_fixtures.sh after

     things that do NOT belong here:
       - stack and commands        -> factory/context.md
       - architecture rules        -> factory/adr/
       - lessons from past runs    -> factory/memory.md
-->
